(
    h1("Create Element")
    hr

    pre(
        a[href="https://github.com/Nektro/modules.js/blob/master/src/create_element.js"]("modules.js/src/create_element.js")
    )
    hr

    p("The Create Element module is a single function module that exposes the "code("create_element")" function. Eventually this may be merged into a more generalized module.")
    hr

    h2("Functions")
    dl(
        dt(code(a[href="#/mod/create_element/create_element"]("create_element()")))
        dd("Provides an easy to use function for creating DOM elements in JavaScript")
    )
    br
    hr
)
